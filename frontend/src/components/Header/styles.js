import styled from 'styled-components';

export const Container = styled.div`
  background: rgba(0, 0, 0, 0.8);
  padding: 0 60px;
  height: 50px;
  display: flex;
  align-items: center;

  a {
    font-weight: bold;
    font-size: 15px;
    color: #fff;
    padding: 0 8px;
  }
`;
